<h1>Admin &gt; Post &gt; list</h1>
<a href="/admin/post/new">New post</a>
<br><br>
<% if @posts.blank? %>
	<h4>There are no post.</h4>
<% else %>
<table class="w3-table w3-bordered w3-striped w3-border w3-hoverable">
	<thead class="w3-red">
		<td>Title</td>
		<td>Content</td>
		<td></td>
		<td></td>
	</thead>
	<% @posts.each do |post| %>
	<tr>
		<td><a href="/admin/post/<%= post.id %>"><%= post.title %></a></td>
		<td><%= post.content.gsub(/(?:\n\r?|\r\n?)/, '<br>').html_safe %></td>
		<td><a href="/admin/post/<%= post.id %>/edit">Edit</a></td>
		<td>
			<%= link_to 'Delete', {:controller=>:admin_post,:action=>:destroy, :id=>post.id}, :method=>:delete, data: { confirm: 'Are you sure to delete?' }, :title => "Delete #{post.id}" %>
		</td>
	</tr>
	<% end %>
</table>
<% end %>
<br>
<a href="/admin/post/new">New post</a>
